# NovelAI Vibe 转智绘姬

一个纯前端网页工具，用于把 NovelAI 官网导出的 vibe bundle 转换为智绘姬可导入的 vibe-group JSON。

## 功能

- 本地浏览器内转换，不上传文件
- 支持 `.naiv4vibebundle.json` / `.json` 文件
- 导出智绘姬需要的 `groups`、`vibeData`、`vibePresets`、`presetImages` 结构
- 官网 vibe 不含原图时，自动使用透明占位图

## 使用

打开网页，选择 NovelAI 官网导出的 vibe 文件，点击转换并下载。
